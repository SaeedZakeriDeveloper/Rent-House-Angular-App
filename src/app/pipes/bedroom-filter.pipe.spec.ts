import { BedroomFilterPipe } from './bedroom-filter.pipe';

describe('BedroomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BedroomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
